<% include ../partials/header %>
<head>
    <link rel="stylesheet" href="/stylesheets/Show.css">
</head>

<div class="container">
<div class="col-md-9" style="margin-left: 125px;">
    <div class="thumbnail">
        <div class="caption-full">
            <h4 class="pull-right">$
                <%= ltcSell.price %>
            </h4>
            <h3>
                <a>
                    <%= ltcSell.title %>
                </a>
            </h3>
            <p>
                <%= ltcSell.description %>
            </p>
            <p><em>Submitted by: <a href="/users/<%= ltcSell.author.id %>"><%= ltcSell.author.username %></a></em></p>
            <% if(currentUser && ltcSell.author.id.equals(currentUser._id)){ %>
                <a class="btn btn-warning" href="/listings/ltcSell/<%= ltcSell._id %>/edit">Edit</a>
                <form id="delete-form" action="/listings/ltcSell/<%= ltcSell._id %>?_method=DELETE" method="POST">
                    <button class="btn btn-danger"><span class="glyphicon glyphicon-trash"></span></button>
                </form>
                <% } %>
        </div>
    </div>  
</div>
</div>

<% include ../partials/footer %>