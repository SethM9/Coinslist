<% include ../partials/header %>
<head>
    <link rel="stylesheet" href="/stylesheets/Show.css">
</head>

<div class="container">
<div class="col-md-9" style="margin-left: 125px;">
    <div class="thumbnail">
        <div class="caption-full">
            <h4 class="pull-right">$
                <%= btcSell.price %>
            </h4>
            <h3>
                <a>
                    <%= btcSell.title %>
                </a>
            </h3>
            <p>
                <%= btcSell.description %>
            </p>
            <p><em>Submitted by: <a href="/users/<%= btcSell.author.id %>"><%= btcSell.author.username %></a></em></p>
            <% if(currentUser && btcSell.author.id.equals(currentUser._id)){ %>
                <a class="btn btn-warning" href="/listings/btcSell/<%= btcSell._id %>/edit">Edit</a>
                <form id="delete-form" action="/listings/btcSell/<%= btcSell._id %>?_method=DELETE" method="POST">
                    <button class="btn btn-danger"><span class="glyphicon glyphicon-trash"></span></button>
                </form>
                <% } %>
        </div>
    </div>  
</div>
</div>

<% include ../partials/footer %>